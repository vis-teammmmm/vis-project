## 数据可视化短学期大作业：`project-2-7-18`

### 1. 环境配置：

```bash
npm install
```
### 2. 使用方法：

#### 2.1 运行后端：

```bash
cd server
node app.js
```
#### 2.2 运行前端：
另外打开一个终端，运行：

```bash
vue serve
```
用浏览器打开运行结果中给出的地址即可看到可视化界面。